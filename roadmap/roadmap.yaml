schema_version: 1
organization: open-science-pillars
last_reviewed: 2026-09-13

repositories:
  - marketplace
  - core
  - ocean-science
  - hydrology
  - tutorials
  - plugin-template
  - knowledge-template
  - nasa-daac-knowledge
  - evals
  - build-kit
  - .github
  - archive-observatory
  - agent-evals
  - precipitation
  - land-ice
  - sea-ice
  - solid-earth
  - land-surface
  - atmospheric-composition
  - atmospheric-physics
  - land-ecosystems
  - ocean-biology
  - composites
  - partner-knowledge

labels:
  roadmap:
    color: "1D76DB"
    description: "Tracked in the Open Science Pillars organization roadmap"
  priority:P0:
    color: "B60205"
    description: "Immediate priority"
  priority:P1:
    color: "D93F0B"
    description: "Near-term priority"
  priority:P2:
    color: "FBCA04"
    description: "Later priority"

decisions:
  - id: ai4s-2026
    title: AI-for-Science submission
    status: declined
    decided: 2026-07-15
    rationale: "The project owner decided not to submit."
  - id: pillar-means-sphere
    title: Pillar means sphere (ADR A)
    status: proposed
    decided: 2026-09-11
    rationale: "Pillars are the five Earth science spheres; disciplines are domain capabilities inside them; provider bundles stay a separate authority axis; classification is canonical under .osp/repository.yaml. Record: marketplace docs/decisions/adr-a-pillar-means-sphere.md; accepted when it merges."
  - id: multi-runtime-packaging
    title: One governed capability, projected to runtimes (ADR B)
    status: proposed
    decided: 2026-09-11
    rationale: "KNOW, ACT, PROVE and REACH are authored once; build-kit renders a Claude projection and an Agent Plugins 1.0 projection; Claude Code, Cowork and Codex are qualification targets; no per-client adapters. Record: marketplace docs/decisions/adr-b-multi-runtime-capability-packaging.md; accepted when it merges."
  - id: one-state-roadmap
    title: One state model for the roadmap
    status: accepted
    decided: 2026-09-13
    rationale: "The proposal-state axis, the seeded proposal issues, the decision labels and the reconcile step were built for a federation of maintainer teams the organization does not yet have. roadmap.yaml stays the single source of truth with one status per deliverable (proposed, active, blocked, done, declined); an active deliverable has an issue in its repository, opened when work starts and closed by the pull request that finishes it; the tool never writes issues. Revisit trigger: adopt GitHub Projects as a rendered view when a second maintainer team declines to edit the YAML or a partner asks for a board; render it from the YAML, one way."

initiatives:
  - id: truth-reconciliation
    title: Federated roadmap and truth reconciliation
    outcome: "The organization has a team-safe roadmap harness and public documentation that matches repository reality."
    status: active
    priority: P0
    depends_on: []
    gate: null
    deliverables:
      - id: roadmap-harness-v1
        title: Build the federated roadmap harness
        repo: build-kit
        status: active
        priority: P0
        depends_on: []
        gate: null
        acceptance:
          - "Roadmap YAML validates and renders deterministically."
          - "Claude and Codex can audit and brief roadmap work."
          - "All GitHub writes are dry-run unless explicitly confirmed."
        evidence:
          - "docs/roadmap-harness-plan.md"
          - "build-record/roadmap-implementation-guide.md"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Python, GitHub CLI, organization governance]
        labels: [roadmap, priority:P0]
      - id: federated-governance-baseline
        title: Declare repository-level roadmap authority
        repo: .github
        status: active
        priority: P0
        depends_on: [roadmap-harness-v1]
        gate: null
        acceptance:
          - "Every repository declares maintainers and proposal policy."
          - "CODEOWNERS covers every repository."
          - "Interim solo review and future team handoff are documented."
        evidence: []
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [GitHub governance]
        labels: [roadmap, documentation, priority:P0]
      - id: reconcile-project-record
        title: Reconcile specifications, progress, limitations, and counts
        repo: marketplace
        status: active
        priority: P0
        depends_on: [roadmap-harness-v1]
        gate: null
        acceptance:
          - "Completed work is not described as deferred."
          - "AI-for-Science is recorded as declined."
          - "Counts, versions, and current limitations match repositories."
        evidence: []
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [OSP architecture, documentation review]
        labels: [roadmap, documentation, priority:P0]
      - id: codex-surface-policy
        issue:
          number: 2
          url: https://github.com/open-science-pillars/marketplace/issues/2
        title: Define Codex as a supported distribution and behavior runtime
        repo: marketplace
        status: proposed
        priority: P1
        depends_on: [reconcile-project-record]
        gate: null
        acceptance:
          - "Architecture and specification distinguish Claude surfaces from Codex distribution."
          - "Testing and support claims are explicit."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [OSP architecture, Codex plugins]
        labels: [roadmap, documentation, priority:P1]

  - id: codex-distribution
    title: Native Codex distribution
    outcome: "Core, ocean-science, and hydrology install through a native Codex marketplace without duplicating source trees."
    status: proposed
    priority: P1
    depends_on: [truth-reconciliation]
    gate: "Re-scoped by the multi-runtime-packaging decision (ADR B): Codex consumes the Agent Plugins projection that build-kit renders from canonical source rather than a hand-written manifest per repository. The per-repository manifest deliverables close or re-scope when osp-architecture-alignment lands its runtime renderer (r1-runtime-renderer); their clean-install intent survives as Codex qualification."
    deliverables:
      - id: codex-core-manifest
        title: Package core as a Codex plugin
        repo: core
        status: declined
        priority: P1
        depends_on: [codex-surface-policy]
        gate: "Superseded in form by r1-runtime-renderer: the Agent Plugins projection is rendered from canonical source (plugin.json, mcp.json at the repository root), so no hand-written .codex-plugin manifest is added; the clean-install intent survives as Codex qualification in r2-core-reference and r3-dependency-reference."
        acceptance:
          - "A valid .codex-plugin/plugin.json is present."
          - "A clean Codex install exposes all core skills."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [JSON, Codex plugin packaging]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: codex-ocean-manifest
        title: Package ocean-science as a Codex plugin
        repo: ocean-science
        status: declined
        priority: P1
        depends_on: [codex-surface-policy]
        gate: "Superseded in form by r1-runtime-renderer: the Agent Plugins projection is rendered from canonical source (plugin.json, mcp.json at the repository root), so no hand-written .codex-plugin manifest is added; the clean-install intent survives as Codex qualification in r2-core-reference and r3-dependency-reference."
        acceptance:
          - "A valid .codex-plugin/plugin.json is present."
          - "A clean Codex install exposes all ocean-science skills."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [JSON, Codex plugin packaging]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: codex-hydrology-manifest
        title: Package hydrology as a Codex plugin
        repo: hydrology
        status: declined
        priority: P1
        depends_on: [codex-surface-policy]
        gate: "Superseded in form by r1-runtime-renderer: the Agent Plugins projection is rendered from canonical source (plugin.json, mcp.json at the repository root), so no hand-written .codex-plugin manifest is added; the clean-install intent survives as Codex qualification in r2-core-reference and r3-dependency-reference."
        acceptance:
          - "A valid .codex-plugin/plugin.json is present."
          - "A clean Codex install exposes all hydrology skills."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [JSON, Codex plugin packaging]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: codex-plugin-template
        title: Add dual Claude and Codex packaging to plugin-template
        repo: plugin-template
        status: declined
        priority: P1
        depends_on: [codex-core-manifest]
        gate: "Superseded in form by r1-runtime-renderer: the Agent Plugins projection is rendered from canonical source (plugin.json, mcp.json at the repository root), so no hand-written .codex-plugin manifest is added; the clean-install intent survives as Codex qualification in r2-core-reference and r3-dependency-reference."
        acceptance:
          - "A newly scaffolded plugin validates on Claude and Codex."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Plugin scaffolding]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: codex-marketplace-catalog
        title: Publish the native Codex marketplace catalog
        repo: marketplace
        status: declined
        priority: P1
        depends_on: [codex-core-manifest, codex-ocean-manifest, codex-hydrology-manifest]
        gate: "Superseded in form by r1-runtime-renderer: the Agent Plugins projection is rendered from canonical source (plugin.json, mcp.json at the repository root), so no hand-written .codex-plugin manifest is added; the clean-install intent survives as Codex qualification in r2-core-reference and r3-dependency-reference."
        acceptance:
          - "The Codex marketplace installs every accepted plugin from its repository root."
          - "Clean-install and update evidence is recorded."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Codex marketplace schema]
        labels: [roadmap, enhancement, help wanted, priority:P1]

  - id: powered-ablation
    title: Refined powered knowledge-bundle ablation
    outcome: "The pre-registered knowledge-effectiveness gate has a reproducible published result and pass-or-pivot decision."
    status: proposed
    priority: P0
    depends_on: [truth-reconciliation]
    gate: null
    deliverables:
      - id: ablation-protocol-amendment
        issue:
          number: 4
          url: https://github.com/open-science-pillars/marketplace/issues/4
        title: Publish the post-migration ablation amendment
        repo: marketplace
        status: proposed
        priority: P0
        depends_on: [reconcile-project-record]
        gate: null
        acceptance:
          - "The refined cases, model, grading, sample size, and decision rule are recorded before the powered run."
        evidence: []
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Experimental design, OSP pre-registration]
        labels: [roadmap, documentation, priority:P0]
      - id: ablation-runner-calibration
        issue:
          number: 2
          url: https://github.com/open-science-pillars/evals/issues/2
        title: Calibrate non-obvious probes and release-mixing grading
        repo: evals
        status: proposed
        priority: P0
        depends_on: [ablation-protocol-amendment]
        gate: null
        acceptance:
          - "Every powered probe discriminates concept application rather than keyword mention."
          - "Release-mixing grading is resolved before the run."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [Evaluation design, scientific rubrics]
        labels: [roadmap, enhancement, priority:P0]
      - id: powered-ablation-run
        issue:
          number: 3
          url: https://github.com/open-science-pillars/evals/issues/3
        title: Run and publish the powered ablation
        repo: evals
        status: proposed
        priority: P0
        depends_on: [ablation-runner-calibration]
        gate: null
        acceptance:
          - "Raw transcripts, commit SHAs, model version, intervals, and infrastructure errors are retained."
          - "Results publish regardless of direction."
          - "A pass-or-pivot decision is recorded."
        evidence: []
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Credentialed compute, evaluation operations]
        labels: [roadmap, priority:P0]

  - id: activation-evals
    title: Skill activation evaluations
    outcome: "OSP measures whether the right skill activates separately from whether the answer is scientifically correct."
    status: proposed
    priority: P1
    depends_on: [codex-distribution]
    gate: null
    deliverables:
      - id: activation-eval-runner
        issue:
          number: 4
          url: https://github.com/open-science-pillars/evals/issues/4
        title: Build and pilot the activation-eval runner
        repo: evals
        status: proposed
        priority: P1
        depends_on: [codex-marketplace-catalog]
        gate: null
        acceptance:
          - "Positive, negative, and ambiguous prompt cases are supported."
          - "The runner observes skill use rather than answer keywords alone."
          - "Claude Code and Codex pilot results are recorded."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Python, agent evaluation]
        labels: [roadmap, enhancement, help wanted, priority:P1]

  - id: release-validation
    title: Release and external validation
    outcome: "OSP has truthful surface evidence, external validation, governed releases, and citable tags."
    status: proposed
    priority: P1
    depends_on: [powered-ablation, codex-distribution]
    gate: null
    deliverables:
      - id: conversational-surface-validation
        issue:
          number: 5
          url: https://github.com/open-science-pillars/marketplace/issues/5
        title: Complete Cowork qualification of a release
        repo: marketplace
        status: proposed
        priority: P1
        depends_on: [reconcile-project-record]
        gate: null
        acceptance:
          - "Core, ocean, and hydrology matrices record tested behavior without overclaiming computation."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [Cowork access, Claude Science access]
        labels: [roadmap, documentation, priority:P1]
      - id: tutorial-nonauthor-validation
        issue:
          number: 2
          url: https://github.com/open-science-pillars/tutorials/issues/2
        title: Complete non-author Tutorial 2 validation
        repo: tutorials
        status: proposed
        priority: P1
        depends_on: [reconcile-project-record]
        gate: null
        acceptance:
          - "One non-author completes Tutorial 2 unaided and friction notes are recorded verbatim."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Earthdata Login, ocean Python environment]
        labels: [roadmap, help wanted, priority:P1]
      - id: provider-steward-handoff
        issue:
          number: 2
          url: https://github.com/open-science-pillars/nasa-daac-knowledge/issues/2
        title: Complete or accurately reframe provider stewardship
        repo: nasa-daac-knowledge
        status: proposed
        priority: P1
        depends_on: [reconcile-project-record]
        gate: null
        acceptance:
          - "A provider steward accepts the documented handoff, or public claims state interim single-steward reality."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [PO.DAAC relationship, knowledge stewardship]
        labels: [roadmap, documentation, priority:P1]
      - id: governed-release
        issue:
          number: 2
          url: https://github.com/open-science-pillars/build-kit/issues/2
        title: Cut the first governed, citable OSP release
        repo: build-kit
        status: proposed
        priority: P1
        depends_on: [powered-ablation-run, conversational-surface-validation]
        gate: null
        acceptance:
          - "Release gates pass in every affected repository."
          - "Version tags and citation metadata agree."
          - "Archive claims are backed by actual archives."
        evidence: []
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Release administration, Zenodo]
        labels: [roadmap, priority:P1]

  - id: phase3-remote-sensing
    title: Conditional remote-sensing and flood workflow
    outcome: "A transparent baseline and optional model-backed flood workflow test the Phase-3 architecture."
    status: blocked
    priority: P2
    depends_on: [powered-ablation]
    gate: "The powered ablation must clear the pre-registered Phase-3 go condition."
    deliverables:
      - id: phase3-flood-slice
        title: Build the baseline-first remote-sensing flood slice
        repo: marketplace
        status: blocked
        priority: P2
        depends_on: [powered-ablation-run]
        gate: "Do not start until the powered-ablation decision authorizes Phase 3."
        acceptance:
          - "A transparent non-model baseline precedes the optional hosted-model method."
          - "Validation, uncertainty, goldens, and evals cover both methods."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [Remote sensing, flood mapping, Phase-3 authorization]
        labels: [roadmap, priority:P2]



  - id: hydrology-investigations
    title: Hydrology for investigations
    outcome: "The hydrology plugin answers basin and event questions end to end with receipts, closes a basin water balance as an attested computation, and survives the USGS WaterServices decommission, released and installed."
    status: active
    priority: P0
    depends_on: []
    gate: "The Phase-2 reading is recorded as a dated addition to docs/phase2-preregistration.md before any provider-product concept is written."
    deliverables:
      - id: hydro-usgs-waterdata-migration
        title: Migrate the USGS connector to the Water Data APIs and release it
        repo: core
        status: done
        priority: P0
        depends_on: []
        gate: null
        acceptance:
          - "The six hydrology eval cases pass on the new connector at the stated N."
          - "A recorded live probe on the new endpoints fixes the field map, paging, and depth facts."
          - "Value-level parity against legacy captures is recorded; the legacy endpoint remains only as a dated deprecation fact."
          - "Core and hydrology are tagged and the hydrology pin points at the tagged core commit."
        evidence:
          - "core--v0.4.2: the observations server on the USGS Water Data API (core PR #22, #23)"
          - "hydrology--v0.5.0: the USGS skills, fixtures and concepts on the Water Data API, pinned at core 0.4.2"
        contributor:
          readiness: needs-context
          champion: owner
          prerequisites: [USGS Water Data APIs, observations server contract, dataretrieval waterdata module]
        labels: [roadmap, priority:P0]
      - id: hydro-basin-unit
        title: Add basin delineation as the unit of analysis
        repo: hydrology
        status: done
        priority: P0
        depends_on: []
        gate: null
        acceptance:
          - "Gauge, point, and hydrologic-unit inputs return a frozen polygon with provenance."
          - "The unsnapped-point trap has an eval case registered in the runner's hydrology manifest and run at the stated N."
          - "Three fixture basins are compared against the monitoring-locations drainage area with a stated tolerance."
        evidence:
          - "hydrology--v0.6.0: the delineate-basin skill with its four signed concepts; nldi-unsnapped-point registered in the evals hydrology manifest and run at N=5"
        contributor:
          readiness: ready
          champion: owner
          prerequisites: [NLDI, Watershed Boundary Dataset, equal-area projections]
        labels: [roadmap, priority:P0]
      - id: hydro-p-et-connectors
        title: Add IMERG precipitation and MOD16 and OpenET evapotranspiration with their trap sets
        repo: hydrology
        status: done
        priority: P0
        depends_on: [hydro-basin-unit]
        gate: "The Phase-2 dated addition is merged before the IMERG and MOD16 concepts are written."
        acceptance:
          - "Run-mixing, cold-season, MOD16 fill, and OpenET area-cap gotchas each carry an eval case registered in the runner's hydrology manifest and run at the stated N."
          - "One live pull per product is recorded; the IMERG Final gap is a dated fact with stale_after."
          - "Provider-product facts carry upstream pending naming their canonical provider directory."
        evidence:
          - "hydrology--v0.6.0: IMERG V07 precipitation and MOD16 and OpenET evapotranspiration with the signed concepts; the IMERG and evapotranspiration cases registered in the evals hydrology manifest"
        contributor:
          readiness: needs-context
          champion: owner
          prerequisites: [GES DISC and LP DAAC access through earthaccess, OpenET API, IMERG run semantics]
        labels: [roadmap, priority:P0]
      - id: hydro-w1-basin-balance
        title: Close a basin water balance as an attested computation
        repo: hydrology
        status: done
        priority: P0
        depends_on: [hydro-usgs-waterdata-migration, hydro-basin-unit, hydro-p-et-connectors]
        gate: null
        acceptance:
          - "The verification notebook runs green on the Ohio, Lees Ferry, and the Roaring Fork refusal."
          - "The storage term refuses below the footprint floor the computation states; no gain factor is applied above it."
          - "Receipts name product versions, runs, captures, and data-tree stamps; the attester recomputes from the receipt."
          - "Hydrology is released with the recipe, skill, and a quickstart walkthrough on Lees Ferry."
        evidence:
          - "hydrology--v0.6.0 and hydrology--v0.6.1: the attested basin water balance, recipe, skill, golden and cases, the release log corrected at 0.6.1"
        contributor:
          readiness: owner-only
          champion: owner
          prerequisites: [Attested computation pattern, GRACE mascon handling, frozen fixture trees]
        labels: [roadmap, priority:P0]
      - id: hydro-w2-w5-workflows
        title: Event reconstruction, flood frequency, drought anatomy, and the reservoir ledger
        repo: hydrology
        status: done
        priority: P1
        depends_on: [hydro-w1-basin-balance]
        gate: null
        acceptance:
          - "Each workflow runs from a basin identifier with receipts and a green verification notebook, except as stated for flood frequency."
          - "Every high-severity gotcha added carries an eval case; DSWx concepts enter the podaac bundle."
          - "Flood frequency exports a WATSTORE file for PeakFQ, parses its output, and refuses a return period without an interval; the export and screening steps have a green notebook, the parse is verified against a PeakFQ output the analyst produces."
        evidence:
          - "hydrology--v0.7.0: the Tulare event reconstruction, annual peaks with the WATSTORE hand-off and screening fit, the five-panel drought anatomy, and the Lake Powell reservoir ledger, each with a golden and its cases registered in the evals hydrology manifest"
          - "nasa-daac-knowledge PR #111 and #112: the four OPERA DSWx concepts entered the podaac bundle and were signed by the steward"
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [OPERA DSWx, PeakFQ, SNODAS, National Water Model retrospective]
        labels: [roadmap, priority:P1]
      - id: hydro-scout-and-confrontation
        title: Teach the scout to plan by workflow and confront SWOT water surface elevation with gauge stage
        repo: hydrology
        status: done
        priority: P1
        depends_on: [hydro-w2-w5-workflows]
        gate: null
        acceptance:
          - "The scout returns a plan by basin and workflow that cites the concepts binding it and downloads nothing."
          - "A confrontation recipe scores SWOT reach water surface elevation against gauge stage at a steward-accepted pair with intervals and receipts; reach discharge against gauge discharge is reported as a consistency check."
          - "The Hydrocron collection gotcha and the version transition facts enter the hydrology bundle; the core Hydrocron tools take a collection_name parameter and core is released with it."
        evidence:
          - "hydrology--v0.7.0: the SWOT confrontation at Baton Rouge scored on changes with the level difference published and not called a bias; the Hydrocron collection gotcha and the version transition facts in the hydrology bundle"
          - "core--v0.5.0: hydrocron_timeseries takes collection_name and the response names the collection that answered"
        contributor:
          readiness: needs-context
          champion: owner
          prerequisites: [Hydrocron, SWOT hydrology products, confrontation recipe form]
        labels: [roadmap, priority:P1]
      - id: hydro-investigation-tutorial
        title: Write the hydrology investigation tutorial and run it with a non-author reader
        repo: tutorials
        status: proposed
        priority: P1
        depends_on: [hydro-w1-basin-balance, hydro-w2-w5-workflows]
        gate: null
        acceptance:
          - "Water year 2023 is told two ways on a clean catalog install: the balance on Lees Ferry, the event on Tulare."
          - "One non-author hydrologist completes it unaided with friction notes captured."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Hydrology plugin installed from the catalog, Earthdata Login]
        labels: [roadmap, priority:P1]
      - id: hydro-stewards-and-releases
        title: Steward scaffolds, CODEOWNERS scopes, and the general-availability flip
        repo: nasa-daac-knowledge
        status: proposed
        priority: P1
        depends_on: [hydro-w2-w5-workflows, hydro-investigation-tutorial]
        gate: "Provider directories are created only under their own dated addition to the pre-registration."
        acceptance:
          - "A CODEOWNERS path scope selects one steward's source-prefixed concept files."
          - "The plugin flips to general availability on stated criteria: tagged release, goldens green, first-wave concepts signed, one non-author tutorial completion."
          - "Outreach notes to USGS, WWAO, and PO.DAAC are drafted with evidence attached and sent by the owner."
        evidence: []
        contributor:
          readiness: owner-only
          champion: owner
          prerequisites: [Steward playbook, credit derivation, release checklist]
        labels: [roadmap, priority:P1]
      - id: hydro-coastal-water
        title: Coastal composites where hydrology meets ocean science
        repo: ocean-science
        status: proposed
        priority: P2
        depends_on: [hydro-w1-basin-balance]
        gate: null
        acceptance:
          - "Land water storage sits beside the sea level budget as a consistency check with no fourth term."
          - "Compound flood reconstruction reconciles gauge and tide datums before any comparison."
          - "No third bundle or plugin; boundary concepts live in their canonical homes and cross-cite."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [CO-OPS connector, sea level budget convention, SWOT coastal products]
        labels: [roadmap, priority:P2]
      - id: hydro-deferred-connectors
        title: Model context, the PeakFQ fixture, ECOSTRESS, and the SWOT hydrology move
        repo: hydrology
        status: blocked
        priority: P2
        depends_on: [hydro-w2-w5-workflows, hydro-stewards-and-releases]
        gate: null
        acceptance:
          - "The National Water Model retrospective is read as pre-2023 model context, labeled as model output in every receipt."
          - "A PeakFQ round-trip fixture (input file, PeakFQ output, parsed receipt) lands and the flood-frequency parser is verified against it."
          - "ECOSTRESS evapotranspiration is a companion loader with its own concept."
          - "The SWOT hydrology facts move to the podaac bundle with both sides re-signed, if the stewards decided that posture."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [National Water Model on AWS, PeakFQ, ECOSTRESS, the podaac bundle's steward posture]
        labels: [roadmap, priority:P2]

  - id: osp-architecture-alignment
    title: Align OSP spheres, governance, and multi-runtime capability packaging
    outcome: "Pillar means sphere; canonical .osp metadata drives organization and runtime projections; per-product tooling is consolidated; Claude Code, Cowork and Codex consume the same governed scientific capabilities through a Claude projection and an Agent Plugins 1.0 projection."
    status: active
    priority: P1
    depends_on: []
    gate: "The decision records (ADR A, ADR B) and the dated Phase-2 pre-registration amendment merge first. Every other deliverable was gated on the first tranche of hydrology-investigations (its P0 deliverables) being recorded done with evidence; that gate was met on 2026-09-12, owner-confirmed, and the four deliverables carry their release tags as evidence."
    deliverables:
      - id: m0-decision-records
        title: Record the sphere alignment and multi-runtime packaging decisions
        repo: marketplace
        status: done
        priority: P1
        depends_on: []
        gate: null
        acceptance:
          - "ADR A (pillar means sphere) is merged under docs/decisions."
          - "ADR B (multi-runtime capability packaging) is merged under docs/decisions."
          - "The design document the records cite is merged with consistent numbering and passes the wording gate."
        evidence:
          - "marketplace PR #81: docs/decisions/adr-a-pillar-means-sphere.md, adr-b-multi-runtime-capability-packaging.md, and the sphere alignment and multi-runtime design (retired; ADR A and ADR B in the marketplace decisions directory carry the decisions)"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [OSP architecture, governance]
        labels: [roadmap, documentation, priority:P1]
      - id: m0-prereg-amendment
        title: Append the dated Phase-2 pre-registration reading of the architecture work
        repo: marketplace
        status: done
        priority: P1
        depends_on: []
        gate: null
        acceptance:
          - "A dated entry states that sphere metadata, provider-neutral .osp metadata, runtime packaging scaffolding and planned repositories are organizational work that neither triggers nor pre-empts the Phase-2 domain gate."
        evidence:
          - "marketplace PR #81: the 2026-09-11 entry in docs/phase2-preregistration.md"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [OSP pre-registration]
        labels: [roadmap, documentation, priority:P1]
      - id: m1-canonical-metadata
        title: Add the canonical .osp metadata schemas and apply them to every non-archived repository
        repo: build-kit
        status: done
        priority: P1
        depends_on: [m0-decision-records, m0-prereg-amendment]
        gate: null
        acceptance:
          - "Schemas for .osp/repository.yaml, .osp/package.yaml and .osp/surfaces.yaml validate; .osp/governance.yaml gains runtime maintainer teams."
          - "Every existing non-archived repository carries repository.yaml; the sphere view renders from data; expected GitHub topics are computed from it and compared in CI."
          - "No Claude manifest is required as a source of sphere truth."
        evidence:
          - "build-kit: osp/repository.schema.json, package.schema.json, surfaces.schema.json; roadmap/governance-schema.json at version 2; scripts/osp.py validate, topics, sphere-view; tests/test_osp.py; docs/osp-metadata.md; SPHERE-VIEW.md rendered from data"
          - "Every non-archived repository carries .osp/repository.yaml and governance.yaml at version 2; core, ocean-science, hydrology and nasa-daac-knowledge carry package.yaml; the plugin, template and bundle gates run osp.py validate so the Claude manifest is checked against the canonical file"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [OSP architecture, JSON Schema, roadmap harness]
        labels: [roadmap, enhancement, priority:P1]
      - id: m1-concept-spheres
        title: Require sphere tags on scientific concepts
        repo: nasa-daac-knowledge
        status: done
        priority: P1
        depends_on: [m0-decision-records]
        gate: null
        acceptance:
          - "Concept frontmatter accepts spheres (required for scientific concepts) and gcmd (optional)."
          - "The linter rejects a scientific concept with no sphere; every existing concept is tagged and re-signed where the steward requires it."
        evidence:
          - "nasa-daac-knowledge tools/check_okf_v02.py E10 and E11; tools/signature_check.py leaves spheres and gcmd outside the signed text; 104 podaac, 36 hydrology, 5 ocean-science and 9 core concepts tagged, the core bundle declared cross-cutting"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [OKF v0.2, the bundle tools]
        labels: [roadmap, enhancement, priority:P1]
      - id: m1-templates
        title: Teach the templates the canonical metadata and the sphere examples
        repo: plugin-template
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata]
        gate: null
        acceptance:
          - "plugin-template and knowledge-template carry .osp metadata examples and sphere and status placeholders that fail validation until replaced."
        evidence:
          - "plugin-template .osp/ with repository, package, surfaces and governance files and a README section; knowledge-template .osp/ and the spheres key on every example concept; a copied template fails osp.py validate until renamed"
        contributor:
          readiness: ready
          champion: PaulMRamirez
          prerequisites: [Plugin scaffolding]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: m2-badges-consolidation
        title: Retire ecco-budget-badge; the workflow and badge writer move beside the canonical attester
        repo: nasa-daac-knowledge
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata]
        gate: null
        acceptance:
          - "The adopter workflow and badge writer live beside the ECCO heat budget attester in the provider bundle's tools, pinned by that repository's release tag; an adopter's badge still resolves from the attester's verdict and cannot be hand-set."
          - "ecco-budget-badge is archived with a pointer; the specification's repository table and the org profile reflect it."
        evidence:
          - "ecco-budget-badge archived by the owner 2026-09-12; nasa-daac-knowledge tools/ecco_budget_badge.py runs the attester in place and tools/templates/ecco-budget-badge-workflow.yml pins that repository by release tag; the specification's repository table (0.6.14), the org profile and the ocean-science README reflect it"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [Attested computation pattern, GitHub administration]
        labels: [roadmap, enhancement, priority:P1]
      - id: m2-evals-consolidation
        title: Rename ecco-agent-evals to agent-evals, the one benchmark repository, with the ECCO cases in a product subtree
        repo: marketplace
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata]
        gate: null
        acceptance:
          - "The repository is renamed agent-evals; the charter is kept; the ECCO cases, fixtures, scoring and results sit under a product subtree so later products join under the same charter; evals stays the runner, graders and scoreboard."
          - "Cases keep concept_basis, expected behavior and deterministic checks; a re-run against the last published tagged set produces identical results; the core linter's ocean coverage rule and the ocean-science README read cases from the new name."
          - "Historical links redirect or clearly resolve; the specification's repository table and the org profile name the new repository."
        evidence:
          - "Renamed to agent-evals by the owner 2026-09-12; the ECCO cases, fixtures and results sit under ecco/ with the charter kept and scoring shared; every published results entry scores identically after the move; the core knowledge-linter, the evals ocean manifest, ocean-science's README and index, and the specification name the new home"
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [Eval runner, case schema]
        labels: [roadmap, enhancement, priority:P1]
      - id: m3-governance-teams
        title: Create sphere, provider steward and runtime maintainer teams and rewrite CODEOWNERS to teams
        repo: .github
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata]
        gate: "Review-enforcing rulesets stay off until a repository has two maintainers, as GOVERNANCE.md records."
        acceptance:
          - "A foundation team, five sphere teams, a provider steward hierarchy, a composites team and the Cowork and Codex runtime maintainer teams exist; one person may hold several during the interim solo period."
          - "Every non-archived repository uses team CODEOWNERS; provider paths map to provider steward teams."
          - "The composite cross-sphere review rule and the runtime authority boundaries are documented."
        evidence:
          - "build-kit osp/teams.yaml declares the foundation team, five sphere teams, the composites team, the provider steward parent with podaac, esdis and hydrosphere-methods child teams, and three runtime maintainer teams; osp.py teams prints the creation commands and osp.py validate refuses an individual or undeclared owner"
          - "Every non-archived repository names teams only in CODEOWNERS, provider bundle paths map to their steward teams, recipes and computations to the methods stewards, the Claude package files to the runtime maintainers; governance.yaml names the teams"
          - "GOVERNANCE.md in .github documents the four kinds of team, the composite review rule, the runtime authority boundaries, interim membership and the planned-repository rule"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [GitHub governance]
        labels: [roadmap, documentation, priority:P1]
      - id: m4-documentation-alignment
        title: Align the profile, marketplace README, specification, glossary, guides and tutorials to spheres and runtime projections
        repo: marketplace
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata, m3-governance-teams]
        gate: null
        acceptance:
          - "No pillar-means-plugin or three-pillars wording survives; the profile sphere view renders from canonical metadata."
          - "The specification states where the measurement, applications and planetary rows of its repository table go under a sphere taxonomy."
          - "Runtime documentation distinguishes development environment, qualified runtime, package conformance and compatibility testing; contributor docs explain KNOW, ACT, PROVE and REACH and state that portable behavior must have a skill representation."
        evidence:
          - "Org profile: the five spheres as the primary figure, the block rendered by osp.py sphere-view --into from every repository.yaml and checked for drift in the organization audit; no three-pillars wording remains anywhere"
          - "Specification 0.6.15: section 0 opens with the organization, capability and runtime model (0.6 Pillar means sphere and the four planes, 0.7 canonical metadata and projections, 0.8 the four runtime words), the packaging matrix records status per runtime, and 1.1 places the measurement, applications and planetary rows"
          - "Marketplace: README positioning, runtime and planned tables, GLOSSARY (sphere, discipline, capability, provider bundle, composite, planned, the four planes, runtime), CONTRIBUTING (skill representation rule, where a contribution goes), steward playbook (runtime independence, spheres, team handoff), the runtime testing guide, docs/runtime-distribution.md and docs/package-authoring-guide.md, ARCHITECTURE 9.10; READMEs of core, ocean-science, hydrology, nasa-daac-knowledge, evals, archive-observatory and the tutorials index state sphere identity and runtime status"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [OSP architecture, documentation review]
        labels: [roadmap, documentation, priority:P1]
      - id: m5-planned-repositories
        title: Instantiate the eleven planned repositories under the planned convention
        repo: .github
        status: done
        priority: P2
        depends_on: [m3-governance-teams, m4-documentation-alignment]
        gate: "Creation is administrative; promotion out of planned is governed work under its own dated pre-registration entry."
        acceptance:
          - "partner-knowledge, precipitation, land-ice, sea-ice, solid-earth, land-surface, atmospheric-composition, atmospheric-physics, land-ecosystems, ocean-biology and composites exist with honest banners, repository and governance metadata, team CODEOWNERS and generated topics."
          - "None carries a package or runtime manifest, a catalog entry, a release or a CITATION.cff; none appears installable; the roadmap reports no active domain deliverable in any of them."
        evidence:
          - "The eleven repositories were created on 2026-09-12 and each opened with the planned scaffold as its first commit: the planned banner README stating sphere, discipline, scope, candidate stewards and what promotion takes, LICENSE, .osp/repository.yaml with status planned, .osp/governance.yaml naming its sphere team (composites-maintainers for composites, provider-stewards for partner-knowledge), CODEOWNERS by that team, and a gate that runs osp.py validate and the wording rules; every gate passed on that commit"
          - "osp.py validate refuses a package, a surfaces file, a runtime manifest, a skills tree or a CITATION.cff wherever status is planned, and refuses a planned repository that appears in the marketplace catalog; SPHERE-VIEW.md, the organization profile block, bootstrap.sh and this roadmap's repository list include the eleven as planned; no deliverable is filed against any of them"
          - "Topics (osp plus the plain sphere words) are repository administration the automation cannot write; each repository carries issue 1 with its description and topic set, cross-linked from build-kit issue 17"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [GitHub administration]
        labels: [roadmap, priority:P2]
      - id: r1-runtime-renderer
        title: Render the Claude and Agent Plugins projections from canonical source and detect drift
        repo: build-kit
        status: done
        priority: P1
        depends_on: [m1-canonical-metadata]
        gate: null
        acceptance:
          - "osp render produces the Claude plugin manifest, the Agent Plugins 1.0 plugin.json and mcp.json where REACH is portable, optional OpenAI extension metadata and the release lock from .osp metadata and the skills tree."
          - "osp plugin-check validates the portable package against the pinned Agent Plugins specification version; CI fails on a hand edit to any generated projection."
        evidence:
          - "build-kit #22: osp.py render writes .claude-plugin/plugin.json, .mcp.json, plugin.json and mcp.json from .osp/package.yaml (which gained metadata, reach and extensions) and .osp/repository.yaml; render --check fails on a hand edit, compared as JSON values; on core the rendered Claude files were identical to the hand-written ones"
          - "osp.py plugin-check validates the portable package against the Agent Plugins specification pinned at 1.0.0, its two schemas vendored under osp/agent-plugins/1.0.0/, plus the specification's text rules (single-token command, cwd forms, reserved environment names, URL and header rules, skill discovery and the Agent Skills frontmatter rules, no per-runtime skills tree); osp.py lock writes .osp/release-lock.json with the classification, content and projection digests and the Agent Plugins version; fifteen tests"
          - "core #32, ocean-science #40, hydrology #53, nasa-daac-knowledge #118 and plugin-template #12 carry the rendered projections and the lock, and their gates run render --check, plugin-check and the lock (reported on pull requests, enforced on a release tag); the portable files belong to the Agent Plugins runtime maintainers in CODEOWNERS; specification 0.6.16 (marketplace #88) and the guides describe it; OpenAI extension metadata is a package.yaml extensions block copied through, unused until Codex documents a namespace"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [Agent Plugins 1.0, Claude plugin format, Python]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: r2-core-reference
        title: Qualify core as the reference capability on Claude Code, Cowork and Codex from one SKILL.md
        repo: core
        status: active
        priority: P1
        depends_on: [r1-runtime-renderer, r5-cross-runtime-evals, r4-shared-prove]
        gate: "The Claude Code leg is qualified on the 0.5.1 release candidate (core #39). Remaining: the Cowork and Codex legs, which the owner runs from the checklists in core #41 and #42 (or waives), then the release itself."
        acceptance:
          - "One canonical skill is discovered and invoked on Claude Code, Claude Cowork and, through the Agent Plugins projection, Codex; the shared script and PROVE step run; the same release identity is recorded on each."
          - "Claude Code installs the capability through one normal install after marketplace setup; Cowork presents the capability rather than its internal KNOW and PROVE dependencies."
        evidence:
          - "build-kit scripts/qualify.py runs the qualification matrix a capability's surfaces.yaml requires: headlessly on Claude Code (install from the marketplace, dependency check from the installer's record, skill discovery against the runtime's inventory, skill invocation in slash and conversational form, knowledge resolution, connector health, the verification scripts on the installed tree, the side-effect gate, the release-lock match) and by checklist for a runtime it cannot drive; one record per runtime under .osp/qualification/ with each test's status and evidence"
          - "core 0.5.0 on Claude Code 2.1.269, 2026-09-12, installed from the marketplace: not qualified, prove and receipt blocked on the attester and release-lock failing because the published release predates the lock"
          - "core release candidate (the checkout after the PROVE deliverable) on Claude Code 2.1.269, 2026-09-12, installed from a local catalog: QUALIFIED, every required test passing, the runtime having run the shared executor and the attester having verified its receipt naming the release, its lock and the runtime; recorded in core/.osp/qualification/claude-code.json (core #36); three harness artifacts found and fixed on the way (golden selection, generated files in the lock digest, the working directory a headless session may write in)"
          - "core 0.5.1 release candidate (core #39) on Claude Code 2.1.269, 2026-09-12, installed from a local catalog: QUALIFIED with connector-invocation and side-effect-confirmation added to the required list; the side-effect gate had counted a Bash call the runtime denied as a write, fixed in build-kit #35 (executed calls only, denied attempts listed) and re-run with qualify.py --only; release ticket core #40 closes on the record"
          - "documentation restructure 2026-09-13: the release qualification guide now carries the per-runtime walkthrough, and the runtime vocabulary is settled in the marketplace glossary"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [Cowork access, Codex access]
        labels: [roadmap, enhancement, priority:P1]
      - id: r3-dependency-reference
        title: Validate dependency realization on ocean-science across runtimes
        repo: ocean-science
        status: proposed
        priority: P1
        depends_on: [r2-core-reference]
        gate: null
        acceptance:
          - "Cowork and Codex each resolve core and nasa-daac-knowledge for ocean-science with no KNOW duplication."
          - "A missing required dependency fails explicitly with the floor named and the action suggested."
        evidence: []
        contributor:
          readiness: needs-context
          champion: needed
          prerequisites: [Cowork access, Codex access, dependency model]
        labels: [roadmap, enhancement, priority:P1]
      - id: r4-shared-prove
        title: Feed Cowork and Codex results through the same deterministic attester
        repo: nasa-daac-knowledge
        status: done
        priority: P1
        depends_on: [r1-runtime-renderer]
        gate: "The attester and the receipt convention exist and verify a Claude Code result; a Cowork or Codex result verifying under them is the reference capability's evidence (r2), which depends on this."
        acceptance:
          - "Results produced on both runtimes verify under one attester; the receipt format identifies the capability release."
        evidence:
          - "nasa-daac-knowledge: docs/receipt-identity.md states the capability (name, version, release-lock digest) and runtime blocks every receipt and attestation carries; tools/receipt_identity.py checks them, with --package DIR against a package tree, and runs its selftest in the check routine; the ECCO computations gain the blocks at their next re-attestation"
          - "core: verification/trend_computation.py is the reference executor (the golden notebook's chain on the regenerated synthetic fixture, receipt naming the capability release and the runtime the caller declares, the weighting detector as mutation evidence) and verification/trend_attester.py the deterministic attester (sanctioned code digest, release identity, fixture regenerated and re-hashed, the series checked against it, trend, p value and interval recomputed at 1e-9, evidence and plausibility); knowledge/computations/synthetic-trend.md is the attested computation concept (draft); a tampered series and a wrong release fail; the PROVE step runs in the goldens gate"
          - "build-kit qualify.py: the prove test has the runtime produce the receipt by running the executor from the installed package, then runs the attester; the receipt test reads the attestation and requires PASS naming the installed version and the runtime; core's surfaces.yaml declares the probe"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [Attested computation pattern]
        labels: [roadmap, enhancement, priority:P1]
      - id: r5-cross-runtime-evals
        title: Record capability, release lock, runtime and model on every eval result
        repo: evals
        status: done
        priority: P1
        depends_on: [r1-runtime-renderer]
        gate: "The record format depends only on the release lock the renderer writes; the first Codex result that fills it is produced by r2-core-reference, which depends on this."
        acceptance:
          - "Results carry capability, capability version, release lock, runtime, model, suite, trial count, score, interval and date."
          - "Identical scientific cases run against both runtimes and compare without changing the capability contract."
        evidence:
          - "evals #30: record.py builds the cross-runtime record from the workspace (package version, the release lock's digest and whether it is current per osp.py lock --check, runtime with projection and version, trial and judge models, suite, trials, UTC date); run_evals.py writes it at the top level and echoes capability, version and lock on each case (rate and ci95 are the score and interval); --runtime, --runtime-version, --judge-model and --no-lock-check with defaults that reproduce the previous behavior"
          - "drivers.py holds the headless command per runtime (Claude Code unchanged; Codex from its documented headless form, marked unexercised until the Codex leg of r2 runs it; other runtimes refused, a Cowork result comes from the qualification checklist with the same record); scoreboard.py states what each file recorded and compares two runtimes case by case; cases, manifests and graders unchanged; selftest covers the record, drivers and comparison"
        contributor:
          readiness: ready
          champion: PaulMRamirez
          prerequisites: [Eval runner, Python]
        labels: [roadmap, enhancement, help wanted, priority:P1]
      - id: r6-compat-probes
        title: Add non-blocking compatibility probes for Gemini CLI and Goose
        repo: build-kit
        status: proposed
        priority: P2
        depends_on: [r1-runtime-renderer]
        gate: null
        acceptance:
          - "osp compat installs the portable package or the canonical skills and MCP declarations through each harness's native mechanism and records the result separately from Tier-1 qualification."
          - "No harness-specific scientific content is introduced; a failure is visible and does not block a release."
        evidence: []
        contributor:
          readiness: ready
          champion: needed
          prerequisites: [Gemini CLI, Goose]
        labels: [roadmap, enhancement, help wanted, priority:P2]
      - id: r7-release-qualification
        title: Advertise a runtime as supported only when the qualification harness passes
        repo: build-kit
        status: done
        priority: P1
        depends_on: [r2-core-reference, r4-shared-prove, r5-cross-runtime-evals]
        gate: "The rule is enforced in every package gate and by publish; its first enforcement on a release tag is the next core release. Dependency resolution across runtimes (r3) feeds the same records when its legs run and is not a precondition of the rule."
        acceptance:
          - "osp qualify runs the required matrix per surface and osp publish emits only qualified projections with honest catalog status."
          - "A release whose runtime fails qualification stays valid and does not advertise that runtime."
        evidence:
          - "build-kit osp.py advertise: a surface may say supported only on a qualified record for this exact version and release lock (the development environment is supported by construction, a future or compatibility runtime is outside the required matrix); --check fails a claim without one and warns on a stale record or an unadvertised qualified runtime; --into README.md renders the runtime table between markers and --check --into fails when it is out of date; every package gate runs it"
          - "build-kit osp.py publish: refuses while the metadata, projections, lock, portable package or a support claim is not clean; emits dist/ with the Claude package zip, the Agent Plugins directory only when a runtime that consumes it is qualified (otherwise release.json says conformant and not emitted), and release.json and RUNTIMES.md with the honest status per runtime; on core today: Claude Code qualified, Cowork and Codex not qualified, the portable projection conformant and withheld"
          - "core, ocean-science, hydrology and plugin-template READMEs carry the rendered runtime table; the runtime distribution note states the rule; seven tests"
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Release administration]
        labels: [roadmap, priority:P1]
      - id: r8-maintainer-run-qualification
        title: Qualify a release candidate through tickets the maintainers close, with waivers, and no secret in the automation
        repo: build-kit
        status: done
        priority: P1
        depends_on: [r7-release-qualification]
        gate: null
        acceptance:
          - "A pull request that changes a package version opens one ticket per required surface with no decision for that version, carrying the checklist and the commands; the ticket closes when the record or a waiver lands on the branch; the merge is gated on every required surface having a decision."
          - "A maintainer can release without a surface by recording a waiver naming who, why and when; the surface is not advertised and the release proceeds."
        evidence:
          - "build-kit: osp.py advertise --release requires a record or a waiver for every required surface at the candidate's version and blocks an unqualified, unwaived surface; qualify.py --waive writes the waiver and --candidate builds a local catalog from the checkout so the Claude Code leg runs on a maintainer's machine against what the release ships; release_tickets.py sync opens, updates and closes the tickets and keeps one comment on the pull request with the workflow's own token; sixteen tests"
          - "core, ocean-science, hydrology and plugin-template carry the release-qualification workflow (a version change or the release label makes a pull request a candidate); the marketplace repository carries docs/release-qualification-guide.md, the maintainers' procedure; the runtime maintainer teams from governance.yaml own each ticket"
        contributor:
          readiness: needs-context
          champion: PaulMRamirez
          prerequisites: [Release administration]
        labels: [roadmap, priority:P1]
      - id: r9-headless-ci-qualification
        title: Run the headless qualification legs in the organization's CI on a release candidate
        repo: build-kit
        status: proposed
        priority: P2
        depends_on: [r8-maintainer-run-qualification]
        gate: "Needs a decision to hold a runtime credential in CI, at which scope and who may trigger it, and the first Codex checklist run to confirm how the portable package installs there (build-kit issue 32)."
        acceptance:
          - "A release-candidate workflow runs the Claude Code leg headlessly, uploads the transcripts and commits the record to the release branch; the Codex leg follows once wired into qualify.py."
          - "Cowork stays by checklist; a single failed conversational test is a re-run before a fail, with the transcript kept."
        evidence: []
        contributor:
          readiness: owner-only
          champion: PaulMRamirez
          prerequisites: [Runtime credential decision, Codex install path]
        labels: [roadmap, priority:P2]
        issue: {number: 32, url: "https://github.com/open-science-pillars/build-kit/issues/32"}
